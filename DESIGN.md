# ATLAS — дизайн-контракт

Один источник правды для всех, кто трогает интерфейс. Три исполнителя работают
параллельно по разным файлам и обязаны сойтись в одну систему — значит все
значения ниже берутся отсюда буквально, а не «на вкус».

Продукт — **инструмент чтения данных**, а не лендинг. Плотность и читаемость
важнее эффектности. Всё, что не помогает понять число или связь, — лишнее.

## 0. Жёсткие запреты

- **Эмодзи запрещены везде**: в разметке, в подписях, в alt, в иконках навигации.
  Поле `emoji` в данных остаётся, но интерфейс его **не читает никогда**.
- **Фиолетовый и сиреневый акцент запрещены** — это подпись «ИИ-слоп». Никаких
  неоновых свечений, градиентных заливок текста, радуг.
- **Тёмной темы нет.** Только светлая. Блок `prefers-color-scheme: dark` удаляется.
- **Карточка перестаёт быть контейнером по умолчанию.** Группировка — волосяной
  линией и воздухом. Карточка (поверхность с тенью) допустима только там, где
  подъём над слоем несёт смысл: панель узла, всплывающие элементы карты.
- **Шрифт Inter запрещён.** Ряды из трёх одинаковых карточек — запрещены.
  Центрированные заголовки-«герои» — запрещены.
- **Чистый чёрный `#000` запрещён.**

## 1. Палитра

Нейтраль одного семейства (холодная) плюс **один** акцент. Ничего больше.

```css
--page:          #FAFAFA;   /* фон страницы */
--surface:       #FFFFFF;   /* поднятый слой: панель, поповер */
--surface-sunk:  #F4F4F5;   /* утопленный: поле ввода, полоса диаграммы */

--hair:          #E7E7EA;   /* волосяная линия — основной разделитель */
--hair-strong:   #D2D2D7;   /* граница активного элемента */

--ink:           #18181B;   /* основной текст, числа */
--ink-2:         #52525B;   /* вторичный текст */
--ink-3:         #8A8A93;   /* подписи, единицы, мета */

--accent:        #14568C;   /* единственный акцент: глубокий синий */
--accent-ink:    #0F4272;   /* наведение и активное состояние */
--accent-soft:   rgba(20, 86, 140, 0.08);

--up:            #0B6B4F;   /* рост */
--down:          #9B2C2C;   /* падение */
--warn:          #8A5A0B;   /* оговорка, оценка без источника */
```

Цвет вида узла (страна/сектор/продукт/услуга/технология) живёт **только в
6-пиксельной точке и в 2-пиксельной левой полосе**, никогда — в тексте.

## 2. Типографика

Geist Variable для текста, Geist Mono для чисел. Ставится пакетами
`@fontsource-variable/geist` и `@fontsource-variable/geist-mono`, импорт в
`src/main.tsx`. Никаких внешних CDN.

| роль | размер/интерлиньяж | начертание | трекинг |
|---|---|---|---|
| `--t-display` | 30/34 | 600 | −0.02em |
| `--t-h1` | 22/28 | 600 | −0.015em |
| `--t-h2` | 17/24 | 600 | −0.01em |
| `--t-body` | 14/21 | 400 | 0 |
| `--t-meta` | 12.5/18 | 400 | 0 |
| `--t-micro` | 11/14 | 500 | 0.06em, ВЕРХНИЙ РЕГИСТР |
| `--t-num-l` | 28/32 | 600 | −0.02em, mono, tabular |
| `--t-num-m` | 20/24 | 600 | −0.01em, mono, tabular |
| `--t-num-s` | 15/20 | 500 | 0, mono, tabular |

Правила:

- **Все числа — моноширинным, `font-variant-numeric: tabular-nums`.** Столбцы
  цифр обязаны выстраиваться по разряду.
- Единица измерения («млрд ₽», «мес») — отдельным `<span>` цветом `--ink-3`,
  размером `--t-meta`, не входит в моноширинный блок.
- Абзац не шире **68ch**. Заголовок раздела не шире **20ch**.
- Заголовок первого уровня на странице — один, `--t-display`. Больше не растим:
  иерархию держат вес и цвет, а не кегль.

## 3. Ритм и сетка

```css
--s1: 4px; --s2: 8px; --s3: 12px; --s4: 16px;
--s5: 24px; --s6: 32px; --s7: 48px; --s8: 64px;

--page-max: 1360px;
--pad: 24px;   /* < 768px: 16px */
--radius: 10px;      /* поверхности */
--radius-sm: 6px;    /* мелкие элементы */
--tap: 44px;         /* минимальная цель тапа */
```

Сетка — двенадцать колонок, `gap: var(--s5)`. Раскладки асимметричные:
`7/5`, `8/4`, `5/7`. Симметричные тройки запрещены. На `< 768px` любая
раскладка схлопывается в одну колонку.

## 4. Обязательные классы

Каркасный агент реализует их в `src/styles.css`; остальные двое пишут разметку
**только этими** классами и не заводят своих цветов и отступов.

**Структура**
- `.section` — блок раздела, вертикальный ритм `--s7`.
- `.section-head` — шапка: `.kicker` (микро, верхний регистр, `--ink-3`),
  `.section-title` (`--t-h1`), `.section-lead` (`--t-body`, `--ink-2`, 68ch).
- `.grid` + `.grid--2` `.grid--3` `.grid--73` `.grid--55` — раскладки. `.grid--3`
  только для плотных списков, не для «трёх карточек с фичами».
- `.hair` — горизонтальная волосяная линия.
- `.stack` — вертикальный поток с `gap: var(--s3)`.

**Данные**
- `.stat` / `.stat-num` / `.stat-unit` / `.stat-label` / `.stat-note` — числовой
  блок. **Без рамки и тени**: слева полоса 2px акцентного или видового цвета.
- `.table` / `.table-wrap` — таблица с волосяными линиями, шапка `--t-micro`,
  числовые ячейки моно и выровнены вправо. `.table-wrap` даёт `overflow-x: auto`.
- `.list` / `.list-row` — строки, разделённые волосяной линией, без карточек.
- `.tag` — метка вида или статуса: 11px, верхний регистр, тонкая рамка.
- `.dot` — точка 6px цветом вида узла.
- `.bar` / `.bar-fill` — горизонтальная полоса для долей.

**Действия и состояния**
- `.btn`, `.btn--ghost`, `.btn--icon` — цель тапа не меньше 44px.
- `.field` — поле ввода на `--surface-sunk`.
- `.panel` — единственная поверхность с тенью (панель узла).
- `.skeleton` — состояние загрузки прямоугольниками по размеру контента.
- `.empty` — пустое состояние: строка объяснения и действие, без картинок.

## 5. Иконки

`@phosphor-icons/react`, начертание `regular`, размер 18 (в шапке 20), цвет
`currentColor`. Иконка никогда не несёт смысла в одиночку — рядом всегда текст
или `aria-label`.

Соответствия для навигации: Обзор — `Globe`, Потоки денег — `ArrowsLeftRight`,
Цепочки — `LinkSimple`, Динамика — `ChartLineUp`, Влияние ИИ — `Sparkle`,
Рынок EdTech — `Buildings`.

Страны обозначаются **двухбуквенным кодом моноширинным** (US, CN, EU, IN, JP),
а не флагом. Прочие узлы — иконка по виду плюс точка цвета вида. Карта
соответствий живёт в `src/ui/glyphs.ts` и данных не касается.

## 6. Движение

Инструмент, а не витрина. `MOTION_INTENSITY = 3`.

- Переход: `160ms cubic-bezier(0.16, 1, 0.3, 1)`, только `transform` и `opacity`.
- Наведение: сдвиг на `-1px` либо смена фона на `--accent-soft`.
- Нажатие: `scale(0.98)`.
- `:focus-visible` — обводка 2px `--accent` с отступом 2px. Обязательна везде.
- **Бесконечных анимаций нет.** Ни пульсаций, ни каруселей, ни мерцаний.
- `prefers-reduced-motion: reduce` — переходы гасятся.

## 7. Как проверяется работа

Каждый исполнитель доказывает результат числами, а не словами:

1. `npm run build` — зелёный (в нём валидатор данных).
2. **Ноль эмодзи в DOM**: регуляркой по `document.body.innerHTML` на каждом
   разделе, включая открытую панель узла.
3. **Ноль фиолетового**: собранный CSS не содержит `#4f3fd6`, `rgb(79, 63, 214)`
   и прочих сиреневых значений.
4. Скриншоты 1440×900 и 390×844 по каждому своему разделу.
5. Горизонтального распирания нет: `scrollWidth − clientWidth = 0` на обеих
   ширинах; широкое содержимое скроллится внутри `.table-wrap`.
6. Консоль пустая.
