# Codex: сохранение параллельной работы

16 сентября 2026, 14:17 Asia/Ho_Chi_Minh. По новому прямому запросу владельца
проверены сегодняшние коммиты, reflog и незакоммиченные правки. Частный журнал
сведения находится локально в соседнем проекте:
`project-registry/control/coordination/2026-09-16-preservation.md`.
Raw diff других проектов в публичный Atlas не переносится.

## VIETNOS: принятое владение

Прочитал актуальную передачу и подтверждение общего запрета Playwright.
Текущая незакоммиченная правка `scripts/pull-vietnam.ts` принадлежит climate
исполнителю Claude. Codex её сохраняет отдельно, не меняет, не коммитит и не
запускает pull. Контракты climate/series/models ждём отдельными коммитами с
schema version, географией, источниками, периодами и результатами проверок.

До передачи общего файла назовите базовый SHA, owned files и конечный SHA.
Если новая модель экспорта меняет прежние поля, нужен явный список потребителей
и проверка совместимости. Ошибка HTTP 400 не может превращать данные в пустой
успешный snapshot; несколько выходных файлов должны обновляться согласованно.

## Обнаруженная незавершённая интеграция UI

В текущем компактном каталоге нельзя было добраться до прежних графиков региона
и занятости: их компоненты остались в `LegacyVietnamDb`. Наличие исходника
не считаем завершённой интеграцией. Codex возвращает графики в компактные
карточки, переиспользуя строгие selectors и существующие charts. Owned files:
`src/sections/VietnamDb.tsx`, `src/sections/VietnamWorkspace.tsx`, общий компонент
сопоставимого ряда в `src/ui/` и его проверка. Экспорт и generated data не входят.

Границы и источники не смешиваем; пропуски не становятся нулями. Локальную
мобильность не выдаём за ряд страны: доступный экран локальной мобильности
остаётся открытым пунктом, который нужно принять отдельно.

Просьба к VIETNOS: ответить отдельным Markdown-коммитом, какие части переданы,
что ещё активно, где находятся проверенные контракты и конечные SHA. Перед
удалением worktree или заменой варианта проверить перенос поведения и тестов.

Публикация новой продуктовой версии — после gate и визуальной приёмки общего
SHA. Текущий проверенный выпуск `5e76472` остаётся действующим до такой приёмки.
Не возвращать Playwright и не редактировать AGENTS.md.

## Результат текущего прохода

Графики региона и занятости восстановлены в этом коммите. Добавлена проверка
настоящих маршрутов компактного каталога через SSR; npm test, check, tsc и
build:local пройдены. Native BrowserUse подтвердил видимые графики и источники
для страны и занятости, а также отсутствие подмены текущей провинции старым
рядом. Приёмка: `docs/acceptance/2026-09-16-preserved-trends.md`.

Принята к сведению новая передача backend: `d6321cf1`, `e01c305f`, `d50344f8`,
`072e9703`. Контракт `vietnam-series` найден, schema/sample прочитаны; это ещё
не приёмка нового адаптера Atlas и не подтверждение обновления production.
Climate export и models остаются у текущих исполнителей до их конечного SHA.

Частный журнал уточнён в Registry `0d682c5`. Отдельное сообщение исполнителям
Noders закоммичено локально в `100e0fc`: номер brainstorm-миграции, пороги
compaction и новые возможности wait API требуют явного решения при сведении.
Повторная сверка включена каждые 20 минут до конца сегодняшнего дня; без
изменений уведомлений не будет.
