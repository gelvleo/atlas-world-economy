# Codex: приём передачи климата и сохранение WIP

16.09.2026, повторная сверка после fetch origin. Это только передача и
проверка сохранности; продуктовые файлы, данные и deployment не менялись.

## База и результат

База локального checkout: `33340e71fd98c785c5348a973691e29f4ef87b2c`.
Прочитанная удалённая линия: `3023163f93122bb5e63a5e47cf64b322ce569c66`.
Общий предок: `78130fd`. Результат этой передачи — отдельный docs-коммит,
содержащий только этот файл; его SHA определяется `git log -1 -- <этот файл>`.
Owned files Codex на этом проходе: только этот новый Markdown.

ACK: `33340e7` patch-equivalent `7987d86`; повторно переносить оба не нужно.
В `2b1387b` климат расширен с 51 до 63 регионов, `3023163` фиксирует передачу.
Схема, метрики и единицы совпадают. Это подтверждение исходников; опубликованный
файл на этом проходе заново не проверялся.

## Что важно не затереть

Локальные dirty `public/data/vietnam-climate.json`,
`public/data/vietnam-graph.json`, `src/data/vietnam.generated.ts` сохранены
приватно вне Git. Они не включены в этот docs-коммит.

Локальный climate snapshot содержит 51 регион (08:15 UTC), upstream — 63
(09:07 UTC). Публикация старого dirty snapshot может убрать 12 регионов.
Graph nodes/edges в обеих версиях равны: 1087/5020, отличается generated_at.
Никаких оснований заменять remote целиком локальным checkout здесь нет.

## Перенос поведения

Граф и календарь доступны из VietnamWorkspace; RegionCharts и EmploymentTrend
доступны в компактном каталоге. Проверено чтением достижимых ветвей и npm test:
num, selectors, ComparableTrend, graph, timeline — PASS. После общего предка
изменялись экспорт, generated data и docs; feature/test-код не заменён.
Контракты series/models переданы в Hermes, но адаптеров и UI-потребителей
в Atlas пока нет. Climate JSON тоже пока не имеет отдельного блока UI.

## Короткий ACK от VIETNOS

1. Подтвердите 63-регионный snapshot из `2b1387b`/`3023163` как канонический
   и отсутствие уникальной текущей работы в трёх dirty generated-файлах.
2. Укажите конечные SHA передачи series/models и владельца следующего
   шага адаптеров; передача контрактов не равна завершённому UI.
3. Локальная мобильность и блок климата остаются открытыми пунктами приёмки.

В этом heartbeat запрещены merge/reset/clean/stash, смена веток/worktree и
production-выкатки. Этот docs-коммит оставлен локально для общего checkout;
push не выполнен. Playwright и изменение AGENTS.md по-прежнему запрещены.
